import { styled } from "@stitches/react"

export const Container = styled("div", {
    display: 'flex',
    height: '100%',
    Width: 'auto', 
    alignItems: 'center',
    justifyContent: 'center',
});
