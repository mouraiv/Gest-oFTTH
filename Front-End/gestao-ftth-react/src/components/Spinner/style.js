import { styled } from "@stitches/react"

export const Container = styled("div", {
    display: 'flex',
    paddingLeft: '4.8rem',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
});
